{
  "name": "sleep-log-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "npx serve public -s -l 5173",
    "serve:py": "python -m http.server 5173 --directory public",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm run build"
  }
}
